const connectors = require('./connectors')

test ("takes 2 entered names and enters them into an array, also assigns the chosen colours to the colours array. If no names entered, an alert is displayed. If same colours chose, an alert is displayed.", () => {
    //ARRANGE


    //ACT
    connectors.submitNamesClick()

    //ASSERT
})

